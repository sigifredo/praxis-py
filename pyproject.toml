[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "praxis"
version = "0.1.3"
description = "Utilidades personales en Python"
authors = [{name = "Sigifredo Escobar Gómez", email = "sigifredo89@gmail.com"}]
dependencies = ["pydub"]
